apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Narrow scope: only the function-router Deployment. See
# skaffold/dev/workflow-builder/kustomization.yaml for the rationale.

namespace: workflow-builder

resources:
  - ../../../../../stacks/main/packages/components/workloads/function-router/manifests/Deployment-function-router.yaml

patches:
  - target:
      kind: Deployment
      name: function-router
    path: deployment-dev-patch.yaml
