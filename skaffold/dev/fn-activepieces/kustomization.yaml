apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Narrow scope: only the fn-activepieces Deployment. See
# skaffold/dev/workflow-builder/kustomization.yaml for the rationale.

namespace: workflow-builder

resources:
  - ../../../../../stacks/main/packages/components/active-development/manifests/fn-activepieces/Deployment-fn-activepieces.yaml

patches:
  - target:
      kind: Deployment
      name: fn-activepieces
    path: deployment-dev-patch.yaml
