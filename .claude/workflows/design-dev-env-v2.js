export const meta = {
  name: 'design-dev-env-v2',
  description: 'Design the improved agentic dev-environment system across 3 workstreams',
  phases: [
    { title: 'Design', detail: '3 parallel Plan agents' },
  ],
}

const SCHEMA = {
  type: 'object',
  properties: {
    plan: { type: 'string', description: 'The full phased implementation plan in markdown. File-level specifics. Reuse existing code/patterns wherever possible.' },
    critical_files: { type: 'array', items: { type: 'object', properties: { path: { type: 'string' }, change: { type: 'string' } }, required: ['path', 'change'] } },
    risks: { type: 'array', items: { type: 'string' } },
    verification: { type: 'array', items: { type: 'string' }, description: 'How to verify each phase end-to-end' },
    open_issues: { type: 'array', items: { type: 'string' }, description: 'Things that must be checked live / decisions still open' },
  },
  required: ['plan', 'critical_files', 'risks', 'verification', 'open_issues'],
}

phase('Design')

const BRIEF = `You are designing improvements to an EXISTING, sophisticated agentic dev-environment system. Read files to verify anything you rely on. Repos: /home/vpittamp/repos/PittampalliOrg/stacks/main (GitOps: ArgoCD + argocd-agent hub-spoke, Talos clusters hub/dev/ryzen) and /home/vpittamp/repos/PittampalliOrg/workflow-builder/main (app monorepo: SvelteKit BFF at root + services/* incl. Python workflow-orchestrator (Dapr durable workflows), function-router, mcp-gateway, sandbox-execution-api, dapr-agent-py/cli-agent-py agent runtimes, fn-system Knative).

CURRENT STATE (verified 2026-07-04):
- Tier-1.5 fast loop: sandbox-execution-api (SEA, the privileged dispatcher in ns workflow-builder) provisions per-run dev-server Sandboxes from dev images (skaffold/dev/<svc>/Dockerfile.dev). Agent edits /sandbox/work/repo, runs sync.sh -> POST tar to /__sync -> Vite HMR (BFF, plugin mode) or dev-sync-sidecar -> emptyDir -> uvicorn --reload / tsx watch (sidecar mode) -> live in ~2-5s. Registry: workflow-builder/src/lib/server/workflows/dev-preview-registry.ts (4 services: workflow-builder, workflow-orchestrator, swebench-coordinator, function-router; fields incl. syncMode/syncPaths/needsDapr/functional/adopt*). Dapr-coupled services get "Dapr-shadow" (unique app-id + pubsub-dev + real DB via daprd secret fetch). Interactive handoff: microservice-dev-session workflow -> cli-dev-agent session sharing the run's /sandbox/work (JuiceFS). Dev hub UI at /workspaces/[slug]/dev. Code-version capture (tar-overlay via /__export, git bundles via Files API) -> manual Promote -> PR (docs/code-version-persistence.md, SHIPPED).
- Tier-2 isolation: vcluster previews (stacks packages/components/workloads/workflow-builder-preview-vcluster/: vcluster.yaml, provision.sh, deploy-app.sh, teardown.sh, runner.sh; README is authoritative). Own API server/DNS/Dapr placement (Dapr control plane INSIDE each vcluster - the whole point; prod manifests deploy unmodified, call_child_workflow placement mesh just works). Shared from host: compute, JuiceFS CSI, NATS, OTEL; per-preview DB now CloudNativePG-in-vcluster (VCLUSTER_PREVIEW_DB_MODE=cnpg). BFF self-migrates 84 tables at boot. Strict workflow-data mode runs in previews. Agent CLI runs (claude/codex/agy) work zero-touch incl. credential copy/host-read+boot-lease for single-use-refresh codex tokens. Tailnet exposure wfb-preview-<id>.tail286401.ts.net via host tailscale operator. BFF -> SEA POST /internal/vcluster-preview -> Job runs runner.sh as cluster-admin vcluster-preview-provisioner SA; Job-success == env-ready. Boot = MINUTES. Capacity: ~2.9 cores/~9.4Gi/~21 pods per preview; dev cluster 9 nodes ~114c/~222Gi; ~8-10 concurrent comfortable; VCLUSTER_PREVIEW_MAX=6 (BFF 429s). PREVIEW_DEV_MODE just landed (#3159): wires the preview LB to an ADOPTED dev pod inside the vcluster (dev-preview-registry adopt* fields: adoptService/adoptDeployment/adoptDaprAppId/adoptTlsTerminator; BFF-focused so far). Host-Kueue does NOT admit vcluster-synced pods (webhook vs syncer fight) - capacity governed by capping vcluster count. Host vcluster ns needs pod-security privileged label (agy seccomp Unconfined). Synchronous host-egress observability (MLflow/OTEL) DEADLOCKS workflows in previews - currently neutralized (disabled) by deploy-app.sh step [6].
- kro pilot: stacks .../workflow-builder-preview-vcluster/kro/ - a WorkflowBuilderPreviewEnvironment ResourceGraphDefinition platform API, BUILT but NOT wired ("not referenced by the parent kustomization until kro is installed and tested"). The BFF port PreviewEnvironmentProvisioner ALREADY has two adapters: SandboxExecutionPreviewEnvironmentProvisioner + KroPreviewEnvironmentProvisioner (config PREVIEW_PROVISIONER_ADAPTER=sandbox-execution-api|kro - the ONLY runtime-swappable adapter family today).
- Hexagonal migration: COMPLETE as of 2026-07-04 (~282 refactor commits 07-02..07-04). ~180 port interfaces in src/lib/server/application/ports/ (13 domain files), ~90 Application*Service classes each with colocated *.test.ts (vitest, 2377 tests), single ~1700-line composition root src/lib/server/application/index.ts (getApplicationAdapters, ~90 lazy memoized getters), config seam src/lib/server/application/config.ts with LOCKED families (PERSISTENCE_ADAPTER=postgres only, EVENT_BUS_ADAPTER=dapr-pubsub only, WORKFLOW_SCHEDULER_ADAPTER=dapr-workflow only; index.ts throws otherwise). dependency-cruiser import-boundary CI gates (db-only-in-adapters, routes-no-adapters, application-core-pure warn ratchet w/ 49 edges, 24 frozen legacy shims). services/shared/workflow-data-contract/ = 22 wire fixtures pinning the orchestrator<->BFF workflow-data boundary, consumed by BOTH vitest and orchestrator pytest. Strict mode ladder: previews strict -> dev overlay pinned strict (WORKFLOW_DATA_API_MODE=http, no PG fallback) -> base ConfigMap flip PENDING. Postgres adapter: adapters/postgres.ts ~60 repo classes defaulting to requirePostgresDb(). Drizzle ORM + drizzle migrations (db-migrate init container).
- Outer loop: git push to workflow-builder@main -> GitHub webhook -> hub Tekton buildkit (ONE oversubscribed hub worker, cpu:500m request; registry-backed :buildcache) -> GHCR -> update-stacks-image commits release-pins + regenerates dev/staging overlays (render-workflow-builder-release-overlays.sh, 1317 lines) -> Source Hydrator ("a few minutes") -> GitOps Promoter (dev timer 0s) -> argocd-agent principal pushes to dev -> ArgoCD sync (30s interval). Total ~15-18 min, build-dominated (BFF prod build needs 8GB heap). Ryzen pins are a SEPARATE flat file (workflow-builder-images-ryzen.yaml). Spegel P2P image mirror live fleet-wide (warm layer pulls). Agent pods are unprivileged: no kubectl/RBAC/buildkit/ghcr-push.
- No per-PR/branch preview automation exists (previews launched imperatively via Dev hub UI or SEA API).

DO NOT RE-PROPOSE (explicitly rejected/reverted): shared-namespace multi-service "manual mesh" previews (vcluster replaced it); fire-and-poll dispatch (reverted 5x - call_child_workflow routes via Dapr placement not DNS); warm-pool claim-bind for SANDBOX PODS (infeasible: generic warm pods share one static daprd app-id; per-pod-unique app-ids via mutating webhook deferred) - NOTE this finding is about sandbox pods, NOT whole vclusters; Gitea reintroduction; DevSpace/idpbuilder (retired).

USER DECISIONS (locked in): (1) Unify on Tier-2 vcluster as THE default agent dev environment - invest in warm pool + faster boot + dev-mode-in-preview; Tier-1.5 stays as light fallback. (2) Multi-service simultaneous editing in one preview is a CORE requirement (hexagonal contract changes touch BFF + orchestrator together; both must hot-reload). (3) Build the fake/in-memory adapter family as the innermost loop (instant boot, no Postgres/Dapr). (4) Per-PR/branch-triggered previews are IN SCOPE.

USER PREFERENCES (from memory): risky infra rewrites roll out in PARALLEL behind a flag, never in-place replacement. Prefer NATIVE tool features over bespoke glue (e.g. vcluster's own sleep/snapshot features, CNPG native bootstrap, PGlite, ApplicationSet generators) before writing custom code. Dapr-native-first for Dapr-related capabilities. ryzen = prototype/canary target, dev = prod-like primary. Evaluate architecture by principled fit, not just what's installed.

YOUR OUTPUT: a phased implementation plan (markdown) with file-level specifics, what to reuse, sequencing, per-phase verification, risks, and open issues that need live checks. You may read any files in both repos to verify claims. Your final structured output is consumed by an orchestrating architect who will synthesize three workstream plans into one roadmap - be concrete and complete, no filler.`

const results = await parallel([
  () => agent(`${BRIEF}

YOUR WORKSTREAM (A): MAKE TIER-2 THE FAST DEFAULT — warm pool, boot-time, declarative lifecycle, observability.

Design in detail:
1. **Warm vcluster pool**: N pre-provisioned generic previews claimed at session start. First VERIFY the shape: read vcluster.yaml, provision.sh, deploy-app.sh, runner.sh, and the kro/ RGD. Analyze exactly what is claim-time-personalizable vs bake-time: per-preview CNPG DB + migrated schema, tailnet hostname (TS_HOST), seeded admin user + smoke-webhook keys, CLI credential copy (must run at claim — user-specific), argocd-agent enrollment (VCLUSTER_PREVIEW_ENROLL_MODE=agent). Argue why the sandbox-pod claim-bind infeasibility (shared Dapr app-id under HOST placement) does NOT apply to whole-vcluster pooling (each vcluster owns its placement). Consider vcluster-native sleep/pause/wakeup and snapshot features (vcluster 0.34.x) as the pooling mechanism (paused vcluster ≈ free; resume in seconds) — check what the installed version supports before proposing.
2. **Cold-boot reduction** for pool refill + cache-miss paths: identify what dominates (vcluster CP start, Dapr control-plane pods inside, CNPG cluster init, BFF db-migrate 84 tables, image pulls, deploy-app.sh serialization). Design: schema-template DB (maintained at migration HEAD by a small controller/CronJob; CNPG bootstrap or CREATE DATABASE TEMPLATE) to skip self-migrate; Spegel-warmed image pulls (already live); parallelizing deploy-app.sh steps; anything vcluster-native.
3. **Declarative lifecycle via kro**: finish + wire the WorkflowBuilderPreviewEnvironment RGD behind PREVIEW_PROVISIONER_ADAPTER=kro (adapter already exists — verify KroPreviewEnvironmentProvisioner in workflow-builder adapters/preview.ts). Add TTL/idle-reaping, status conditions, and pool semantics (a Pool CR or just N pre-created CRs + a claim label?). Parallel rollout: kro path behind the existing adapter flag, SEA/runner path stays default until proven.
4. **Preview observability**: stop neutralizing MLflow/OTEL — design an async-safe telemetry path so agents iterating IN the preview still get traces (e.g. OTEL collector inside the vcluster batching to host egress, or fire-and-forget export settings) without the synchronous-export workflow deadlock documented in the README.
5. **Capacity governance**: idle TTL, sleep-after-inactivity, max standing pool size vs VCLUSTER_PREVIEW_MAX, eviction order; interplay with the ~8-10 concurrent ceiling and memory-request-bound scheduling.`, { label: 'plan:fast-tier2', phase: 'Design', schema: SCHEMA, agentType: 'Plan', effort: 'xhigh' }),

  () => agent(`${BRIEF}

YOUR WORKSTREAM (B): MULTI-SERVICE DEV-MODE INSIDE ONE TIER-2 PREVIEW — N services hot-reloading simultaneously, sync normalization, dependency-change lane, contract-change flow.

Design in detail:
1. **Multi-service adopt**: extend the just-landed PREVIEW_DEV_MODE single-service adopt to N services per preview. Read the adopt implementation first (dev-preview-registry.ts adopt* fields; SEA app.py preview-native handling; stacks PREVIEW_DEV_MODE wiring in the vcluster component). Design: per-service dev pods inside the vcluster each adopting that service's Deployment/Service/app-id (scale prod replica to 0, restore on teardown), a single shared repo workspace for the agent (one clone of workflow-builder; services/* subdirs map to each dev pod's syncPaths), and sync fan-out: either sync.sh targets multiple /__sync endpoints keyed by changed path prefix, or an in-vcluster sync-router receiving one tar and distributing per-service slices. Keep the Dapr app-id semantics correct (inside the vcluster the REAL app-ids are fine — no Dapr-shadow needed, argue this).
2. **Sync normalization**: recommend ONE mechanism so adding a service = one registry entry (evaluate: dev-sync-sidecar everywhere vs keep vite plugin for the BFF; auto-derive syncPaths; what /__export means per service for version capture). Cover the unregistered services: mcp-gateway, workflow-mcp-server, sandbox-execution-api itself, fn-system (Knative — what is realistic?), and the Python agent runtimes (dapr-agent-py, cli-agent-py — per-session pods; decide whether they belong in this loop or iterate via the innermost fake loop + image pins).
3. **Dependency-change lane**: HMR cannot absorb package.json/pnpm-lock/pyproject changes. Design the escalation: (a) in-pod dependency install (dev pods run writable; pnpm install/uv sync on local scratch emptyDir — NEVER JuiceFS, npm-install-on-JuiceFS is documented catastrophic ~11min) triggered when sync detects manifest changes; (b) when even that fails (base-image/native deps), an agent-triggerable in-cluster dev-image rebuild — evaluate reusing hub Tekton vs a buildkit on dev/ryzen with the registry cache, exposed through a narrow privileged SEA endpoint (mirror the vcluster-preview Job pattern: privilege concentrated in one SA, BFF only asks). Sequencing + what to defer.
4. **Contract-change flow**: a workflow-data contract change touches BFF TS + orchestrator Python + services/shared/workflow-data-contract fixtures. Design how the in-preview loop runs BOTH sides' contract tests fast (in the dev pods via exec'd test commands? via the strict-mode smoke workflows preview-agent-smoke/preview-data-plane-smoke?) and how the agent gets fast pass/fail.
5. **Session + UX**: one interactive cli-dev-agent session driving N dev-mode services (workspace layout, per-service sync status surfaced in the Dev hub detail page, per-service health/readiness, the preview URL still serving through adopted pods). Teardown semantics restoring all adopted Deployments.`, { label: 'plan:multi-service', phase: 'Design', schema: SCHEMA, agentType: 'Plan', effort: 'xhigh' }),

  () => agent(`${BRIEF}

YOUR WORKSTREAM (C): THE INNERMOST FAKE-ADAPTER LOOP + PER-PR PREVIEWS.

Design in detail:
1. **Fake/in-memory adapter family** unlocking instant no-cluster boot of the BFF application core. The Postgres adapter is ~60 repo classes over Drizzle — do NOT propose hand-writing 60 fakes. Evaluate seriously: PGlite (@electric-sql/pglite, in-process WASM Postgres) as a drizzle driver so the REAL Postgres adapter runs unchanged against an embedded DB (real drizzle migrations at boot, zero fake code, no server) — check drizzle-orm PGlite support and what requirePostgresDb()/db wiring in the repo would need (read src/lib/server/db/ and adapters/postgres.ts:~335). Then: EVENT_BUS_ADAPTER=in-process (EventEmitter-backed implementing the DaprEventBus port surface), WORKFLOW_SCHEDULER_ADAPTER — analyze honestly what an in-process scheduler CAN cover (inline activity execution for simple flows) vs what stays cluster-only (durable timers, child workflows via placement), and scope it to what agents need for logic iteration. Config plumbing: unlock the families in config.ts + index.ts behind e.g. APP_PROFILE=lite, keep index.ts throwing for unknown values, dependency-cruiser rules unaffected. Where it runs: pnpm dev:lite locally AND inside cli-agent sandbox images (no cluster deps) — the innermost loop before any preview. Also: use it to speed vitest (2377 tests) where per-test doubles are today hand-rolled? Keep scope honest.
2. **Contract tests as the innermost gate**: wire services/shared/workflow-data-contract fixtures + vitest + orchestrator pytest into a single fast command agents run in the lite loop before touching a preview.
3. **Per-PR previews**: design GitOps-triggered Tier-2 previews for PRs on workflow-builder. Key insight to evaluate: with multi-service dev-mode (workstream B) a PR preview does NOT need PR-built images — provision a standard Tier-2 preview + dev-mode pods + git checkout of the PR head synced in at boot (seconds, no Tekton build); optionally a flag for image-faithful previews via the existing outer-loop Tekton building pr-<n> tags. Decide the trigger path (GitHub webhook -> existing hub Tekton EventListener -> SEA/kro call, vs a small hub controller, vs ApplicationSet PR generator — note previews are imperative Jobs/kro CRs on dev, hub-spoke argocd-agent in between; pick what fits the existing machinery, prefer native features), status/URL reporting back to the PR (commit status + comment with the tailnet URL), teardown on PR close + TTL, capacity guard (share VCLUSTER_PREVIEW_MAX budget; label PR previews for eviction priority), and auth (previews are tailnet-only — fine).
4. **Loop closure**: agent Promote->PR (shipped) -> PR preview auto-provisions -> Playwright-MCP critic / human verifies against the PR preview URL -> merge -> outer loop. Design the minimal glue so this whole chain is automatic.`, { label: 'plan:fakes-and-pr', phase: 'Design', schema: SCHEMA, agentType: 'Plan', effort: 'xhigh' }),
])

const [fastTier2, multiService, fakesAndPr] = results
return { fastTier2, multiService, fakesAndPr }