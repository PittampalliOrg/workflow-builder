export const meta = {
  name: 'design-agent-cli-runtime',
  description: 'Three Plan agents design the agent-CLI-in-sandbox system from different perspectives',
  phases: [
    { title: 'Design', detail: 'parallel Plan agents: Dapr runtime, K8s/stacks, UX/terminal' },
  ],
}

const PLAN = {
  type: 'object',
  required: ['summary', 'plan'],
  properties: {
    summary: { type: 'string' },
    plan: {
      type: 'array',
      items: {
        type: 'object',
        required: ['step', 'detail'],
        properties: {
          step: { type: 'string' },
          detail: { type: 'string', description: 'Concrete design: new/changed files, function/endpoint names, payload shapes, env vars' },
          files: { type: 'array', items: { type: 'string' } },
        },
      },
    },
    risks: { type: 'array', items: { type: 'string' } },
    decisions: { type: 'array', items: { type: 'string' }, description: 'Key decisions made + rationale; flag anything genuinely user-decidable' },
  },
}

const CONTEXT = `You are designing a new capability for the workflow-builder platform (repo /home/vpittamp/repos/PittampalliOrg/workflow-builder/main, k8s config repo /home/vpittamp/repos/PittampalliOrg/stacks/main). READ-ONLY: do not edit/write anything; produce a design.

GOAL: Run agent CLIs (Claude Code first; Codex + Antigravity/Gemini later) interactively inside Kueue-admitted sandbox pods, as first-class members of the existing durable-agent-runtime ecosystem. USER DECISIONS (fixed): (1) FULL interactive TUI accessed via a web terminal — the user interacts with the real CLI TUI; the Dapr workflow wraps the SESSION LIFECYCLE (not per-turn driving); transcript/status mirrored into session_events so the session appears in the /sessions UI (read-only timeline); (2) Claude Code only in v1, but registry/contract designed generically; (3) auth via PERSONAL SUBSCRIPTION tokens (CLAUDE_CODE_OAUTH_TOKEN from 'claude setup-token', 1-year inference-only) stored per-user encrypted (vaults/app_connections), NOT cluster API keys — note interactive TUI usage stays on subscription limits (the 2026-06-15 Agent SDK credit split only hits -p/SDK); ANTHROPIC_API_KEY must NOT be visible in the pod env or it silently outranks subscription auth. (4) Use HERDR (https://herdr.dev, single static Rust binary, AGPL-3.0+commercial) instead of tmux: headless background server, named persistent sessions, multi-client attach, detach keeps processes alive, and an NDJSON Unix-socket API (~/.config/herdr/herdr.sock or HERDR_SOCKET_PATH): agent.get/list/explain (semantic states working|idle|blocked|done via zero-config foreground-process+output detection with official Claude Code/Codex/Antigravity integrations), pane.send_text/send_keys/run, pane.read, pane.wait_for_output + agent-status waits, events.subscribe (long-lived NDJSON event stream), events.wait, notification.show, workspace/tab/pane/worktree CRUD, pane.report_agent for native state reporting.

ESTABLISHED FACTS from exploration (full structured findings in /tmp/claude-1000/-home-vpittamp-repos-PittampalliOrg-workflow-builder-main/f4c1842d-a9d1-4f66-95aa-3184826dce3f/tasks/wva0h33mh.output — jq keys .result.{runtime,terminalUi,stacks,claudeCli,codexGemini,webTerm}; read it for detail):

RUNTIME CONTRACT: services/shared/runtime-registry.json is the SSOT (5 descriptors; fields id/appIdConfigKey/instancePrefix/family/mainContainerName/imageEnvKey/agentMetadataFramework/benchmarkEligible/capabilities{durabilityGranularity per-activity|per-turn, supportsMcp, supportsHooks, hookTiming, incrementalEvents, ownsSandbox, requiresWarmPool, requiresBrowserSidecars, multiProvider, supportedProviders...}); scripts/sync-runtime-registry.mjs generates orchestrator + BFF copies (drift-guarded). Every runtime registers Dapr workflow 'session_workflow'; bridge sentinel 'agent_workflow'. Contract: childInput {sessionId, agentConfig{modelSpec,maxTurns,permissionMode,mcpServers,hooks,plugins}, instructionBundle.rendered.system, autoTerminateAfterEndTurn}; return {success, output|content, sessionId, agentRuntime,...}; events POSTed to BFF /api/internal/sessions/{id}/events/ingest with CMA vocabulary (session.status_* mandatory; agent.message/tool_use/tool_result; incremental tier message_delta/agent.llm_usage/agent.context_usage/hook.decision iff incrementalEvents). claude-agent-py (services/claude-agent-py/) is the closest precedent: FastAPI main.py registers session_workflow + run_claude_sdk_turn_activity + check_cancellation_activity; endpoints POST /internal/sessions/spawn (StartInstance), /internal/sessions/raise-event, /api/v2/agent-runs/{id}/{terminate,pause,resume} + DELETE purge; session_workflow loops wait_for_external_event('session.user_events') -> check_cancellation -> call_activity(turn, retry=3) with when_any 15-min turn timer; claude_sdk_runner.py builds ClaudeAgentOptions (mcp_servers via mcp_config.py translating agentConfig.mcpServers incl. URL qualification for in-cluster services; include_hook_events); event publisher vendored from services/shared/session_events/publisher.py (producer triple dedup, daemon-thread fire-and-forget POST). Dockerfile.sandbox: FROM openshell-sandbox image + node22 + npm i -g @anthropic-ai/claude-code -> /usr/local/bin/claude + uv sync; user claude-agent uid 10001; uvicorn src.main:app port 8002. Cancellation: Dapr state key session-cancel:{instance_id}. UI-direct spawn: src/lib/server/sessions/spawn.ts (resolves runtime via BFF registry runtime-registry.ts, swap-safety gate swap-safety.ts, rewriteMcpForBrowserSidecar mcp-sidecar.ts, goal MCP auto-wire, provisionSessionSandboxWithRetry then StartInstance via the pod's spawn endpoint, raiseSessionUserEvents for subsequent user messages). Session events: appendEvent with advisory-lock sequence, SSE /api/v1/sessions/[id]/events/stream via PG LISTEN/NOTIFY, Last-Event-ID replay. Lifecycle Controller (src/lib/server/lifecycle/) is the stop/terminate/purge SSOT; runtimes need management parity endpoints.

WEB TERMINAL (exists): xterm.js (@battlefieldduck/xterm-svelte + FitAddon + AttachAddon) components src/lib/components/sandbox/sandbox-terminal.svelte + src/lib/components/sessions/openshell-terminal-tabs.svelte; BFF WebSocket proxy src/lib/server/ws-terminal-proxy.ts (handleUpgrade on /api/openshell/sessions/[sessionId]/terminal/[terminalId] and /api/sandboxes/[name]/terminal/[sessionId]; JWT/cookie auth + resolveOpenShellTerminalTarget scope check; forwards to ws://openshell-agent-runtime.openshell.svc.cluster.local:8084; resize as \\x01-prefixed JSON text frames; exponential-backoff reconnect). NOTE: this terminal targets OPENSHELL-namespace workspace sandboxes via openshell-agent-runtime. There is ALSO a pod-shell kube-exec WS proxy for runtime-pod debug (ws-kube-exec-proxy.ts, SHELLABLE_CONTAINERS/ALLOWED_CONTAINERS allowlists — verify in repo). OpenShell exec has 4KB stdout truncation + no streaming (terminal-sessions are real PTYs though).

K8S/STACKS: per-session sandbox pods via kubernetes-sigs/agent-sandbox v0.4.5 (Sandbox CR, podTemplate; SandboxTemplate/SandboxWarmPool/SandboxClaim extensions). sandbox-execution-api (workflow-builder ns) holds SANDBOX_EXECUTION_CLASSES_JSON with 5 classes (interactive-agent localQueue=interactive-agent priorityClassName=interactive-workload agentHostImage=dapr-agent-py-sandbox cpu 250m/mem 512Mi; benchmark-fast; benchmark-minimal-agent; background-warm; secure-gvisor runtimeClassName). Kueue: ClusterQueues interactive-agent (4CPU/8Gi/16 pods, WorkloadPriorityClass interactive-agent value=1000, cohort agent-platform, PSI memory-pressure AdmissionCheck), benchmark-fast/eval, background-warm, secure-gvisor; pod-native admission via kueue.x-k8s.io/queue-name label injected by openshell-sandbox-dapr-webhook (which also injects daprd annotations dapr.io/enabled,app-id={sandbox-name},config=openshell-sandbox-dapr). Pod shape: init seed-openshell-config (mTLS certs -> ~/.config/openshell/gateways/.../mtls + metadata.json), main agent container, volumes workspace emptyDir + openshell-client-tls/ca secrets, automountServiceAccountToken=true, terminationGracePeriod 90s. OTEL: collector at otel-collector.observability:4317(grpc)/4318(http); traces->MLflow, logs->Loki, metrics scrape. Secrets: ExternalSecrets workflow-builder-secrets (INTERNAL_API_TOKEN, ANTHROPIC_API_KEY...), dapr-agent-py-secrets (openshell ns, all provider keys incl ANTHROPIC_API_KEY — must NOT flow into CLI pods). CronJobs: workflow-builder-sandbox-gc (agent-host-* CRs >4h), lifecycle-terminal-reaper, agent-runtime-idle-reaper. CLAUDE.md invariant: agent sandbox pods MUST colocate with orchestrator namespace (workflow-builder) — Dapr sub-orchestration doesn't cross namespaces. Skills: agent_skill_registry.packageManifest.files materialized by dapr-agent-py _materialize_instance_skill_packages -> /sandbox/skills/. Session outputs: scan /mnt/session/outputs + /sandbox/outputs -> POST /api/internal/sessions/[id]/outputs/ingest.

CLAUDE CODE CLI (researched, mid-2026): interactive TUI is the target mode. Hooks: FIVE types incl native **http** hooks (POST hook-input JSON {session_id, transcript_path, cwd, hook_event_name, tool_name, tool_input, tool_response, prompt,...} to a URL; 2xx JSON processed; failure non-blocking) — first-class event mirroring; rich event list (SessionStart/SessionEnd/UserPromptSubmit/Stop/PreToolUse/PostToolUse/PostToolUseFailure/PermissionRequest/PermissionDenied/SubagentStart/Stop/Notification/PreCompact/PostCompact...); transcript_path = on-disk JSONL tailable for full assistant text + usage. Native OTEL: CLAUDE_CODE_ENABLE_TELEMETRY=1, metrics (claude_code.cost.usage, token.usage, session.count...) + log events (user_prompt, tool_result, api_request w/ cost+tokens) + beta traces (CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1); OTEL_EXPORTER_OTLP_ENDPOINT etc; interactive sessions IGNORE inbound TRACEPARENT (only -p/SDK nest under host trace); session.id attribute on everything. Config: settings.json hierarchy (managed /etc/claude-code/managed-settings.json highest), ~/.claude/skills + plugins cache, .mcp.json / --mcp-config, CLAUDE_CONFIG_DIR relocates ~/.claude. Auth precedence: ANTHROPIC_API_KEY outranks subscription -> keep it OUT of env; CLAUDE_CODE_OAUTH_TOKEN (setup-token, Pro/Max, inference-only) works for interactive TUI; --dangerously-skip-permissions REFUSED as root (run non-root). Remote Control mode exists (claude remote-control exposes session to claude.ai mobile/web via outbound polling; requires FULL claude.ai OAuth creds ~/.claude/.credentials.json, NOT setup-token) — optional add-on, not the primary surface. Background supervisor: claude --bg / claude attach. Codex (later): codex app-server JSON-RPC over --listen ws:// with /readyz, codex exec --json, config.toml mcp_servers, OTEL [otel]; Gemini/agy (later): gemini --acp, settings.json mcpServers, full OTEL; Antigravity agy CLI is Gemini CLI's successor (immature headless: no --output-format json yet).

NAMING CONVENTION: existing services dapr-agent-py/claude-agent-py/adk-agent-py; runtime ids match service names. Propose consistent naming for the new host service + runtime id(s).`

phase('Design')
const [runtime, platform, ux] = await parallel([
  () => agent(CONTEXT + `

YOUR PERSPECTIVE: **Dapr runtime + events architect.** Design the new runtime service end-to-end:
1. The host service (analogous to services/claude-agent-py): process supervision model — how it launches 'herdr server' + the claude TUI pane at session start; FastAPI surface (spawn/raise-event/terminate/pause/resume/purge parity); how it consumes the herdr socket API (events.subscribe) to derive session.status_* (working->status_running, idle/blocked->status_idle variants, done/exit->terminated) and how Claude Code http hooks + transcript_path JSONL tailing produce the CMA event tier (user.message, agent.message, agent.tool_use/tool_result, agent.llm_usage from transcript usage records). Decide hook config mechanism (managed-settings.json baked in image vs settings written at session seed). Decide what runs where: herdr server lifetime, claude launched inside a herdr pane via integration, working dir (/sandbox or /workspace?).
2. The Dapr workflow shape for a LIFECYCLE-wrapping session_workflow: deterministic structure (activities for seed_session/start_cli, then an event loop on wait_for_external_event for control events + durable idle-timeout timers; how turn boundaries from hooks (Stop event) surface; how the workflow ends (SessionEnd hook -> host raises external event); contract compliance (childInput handling, return dict, autoTerminateAfterEndTurn=false semantics for interactive; what durable/run from a workflow canvas would mean for this runtime — support or reject in v1?).
3. runtime-registry.json changes: new descriptor (propose id + family + capabilities; does durabilityGranularity need a new 'per-session' enum value? what breaks in sync script/swap-safety/orchestrator resolve — check those readers), swap-safety treatment (CLI family swaps should likely REJECT).
4. Session seeding: how agentConfig.mcpServers -> .mcp.json (reuse claude-agent-py mcp_config.py translation incl URL qualification + goal MCP auto-wire from spawn.ts), skills materialization into ~/.claude/skills (reuse agent_skill_registry packageManifest pattern), instructionBundle -> CLAUDE.md or --append-system-prompt-file, permissionMode mapping.
5. OTEL: env wiring to the collector; what Session Pulse can show (llm_usage events from transcript tail vs OTEL metrics); MLflow trace story given interactive mode ignores TRACEPARENT.
6. Lifecycle Controller integration: stop/terminate/purge flow for this runtime (cooperative SIGTERM/herdr pane close -> SessionEnd -> workflow completes; purge semantics; reaper compatibility).
Read the actual code (services/claude-agent-py/src/*.py, src/lib/server/sessions/spawn.ts, src/lib/server/agents/*.ts, services/shared/session_events/publisher.py, scripts/sync-runtime-registry.mjs, services/workflow-orchestrator/core/runtime_registry.py + workflows/sw_workflow.py resolve path) to ground every step in real names. Return a concrete step plan.`, { label: 'plan:dapr-runtime', phase: 'Design', agentType: 'Plan', schema: PLAN }),

  () => agent(CONTEXT + `

YOUR PERSPECTIVE: **K8s / stacks / capacity architect.** Design the platform side:
1. Sandbox image: new Dockerfile (probably services/<new-host>/Dockerfile.sandbox FROM the openshell-sandbox base like claude-agent-py does): claude CLI pinned version, herdr static binary (install method + version pinning + AGPL note), host python app, non-root user (required for --dangerously-skip-permissions and good hygiene), HERDR config baked, CLAUDE_CONFIG_DIR strategy, where node lives. Outer-loop build integration (Tekton github-outer-loop per-service triggers, GHCR).
2. SANDBOX_EXECUTION_CLASSES_JSON: new execution class (propose name e.g. interactive-cli) mapped to localQueue=interactive-agent + priorityClassName interactive-workload; resource sizing for a TUI CLI + herdr + host (claude is a node process, recommend cpu/mem/ephemeral); which agentHostImage; agentHostEnv needs. How sandbox-execution-api selects class per runtime (read its code path in the repo if present, or the BFF provisioning call in provisionSessionSandboxWithRetry/agent-workflow-host.ts).
3. Kueue: confirm interactive-agent ClusterQueue capacity is adequate (4 CPU/8Gi/16 pods on profile); whether a NEW queue/class for CLI sessions is warranted vs reusing interactive-agent; KueueCapacityProfiles share adjustments; PSI admission implications for interactive UX (admission delay = user waits — how to surface 'queued' state in UI).
4. Pod shape: which webhook injects what for these pods (openshell-sandbox-dapr-webhook matches openshell + workflow-builder?); daprd sidecar needs (enable-workflow for session_workflow registration, placement, Configuration name); seed-openshell-config init needed? (CLI pods may not need OpenShell gateway access at all in v1 — decide); volumes (workspace emptyDir sizing for repos, ~/.claude persistence within pod lifetime; do we want OPTIONAL PVC for cross-pod session resume later?); automountServiceAccountToken; securityContext non-root.
5. Secrets/auth: per-user CLAUDE_CODE_OAUTH_TOKEN flow — stored where (vaults vs app_connections vs new table; vaults are CMA-parity workspace-scoped), decrypted by BFF at spawn, injected HOW into the Sandbox CR podTemplate env (sandbox-execution-api API shape — does the BFF pass env overrides today? check agentHostEnv / spawn payload), rotation/expiry UX (1-year tokens), and the CRITICAL exclusion: ensure no ANTHROPIC_API_KEY/CLAUDE_API_KEY from shared secret envFrom reaches these pods.
6. GitOps: list the stacks manifests to add/change (execution-classes JSON edit, any new ConfigMap, NetworkPolicy needs — CLI pods need egress to api.anthropic.com + statsig + sentry; OTEL endpoint env), delivery via commit-pin/outer-loop, GC/reaper coverage (sandbox-gc matches agent-host-*; idle reaping for abandoned CLI sessions — propose TTL approach using herdr idle state).
Read the actual manifests in /home/vpittamp/repos/PittampalliOrg/stacks/main (sandbox-execution-api Deployment, kueue-capacity, openshell webhook ConfigMap, ExternalSecrets) and the BFF provisioning code to ground every step. Return a concrete step plan.`, { label: 'plan:k8s-stacks', phase: 'Design', agentType: 'Plan', schema: PLAN }),

  () => agent(CONTEXT + `

YOUR PERSPECTIVE: **UX / terminal access / BFF architect.** Design the user-facing path:
1. Terminal transport: the CLI runs in the per-session agent-sandbox pod in the workflow-builder namespace (NOT an openshell-ns workspace sandbox). Compare and pick: (a) extend the existing pod-shell kube-exec WebSocket proxy (find it: src/lib/server/ws-kube-exec-proxy.ts or similar, SHELLABLE_CONTAINERS allowlist, how it authenticates + spawns) to spawn 'herdr session attach <name>' in the runtime container — apiserver in keystroke path but zero new agents; vs (b) a small WS->PTY endpoint in the host service itself (FastAPI websocket spawning herdr attach via pty) proxied by the BFF like ws-terminal-proxy.ts does for openshell — keeps apiserver out, host already exists; vs (c) ttyd sidecar. Recommend one with rationale; detail the BFF proxy route, auth (JWT + session scope), reconnect/resize handling, and multi-tab support. herdr makes disconnects safe (detach not kill) and supports multi-client attach.
2. Session UI: how a CLI session renders on /workspaces/[slug]/sessions/[id] — terminal-first layout (xterm fills the page) with the event timeline as secondary tab; status chip driven by session.status_* (herdr semantic states incl 'blocked' = waiting for user input -> needs-attention indicator + maybe notification); Session Pulse with whatever usage events we mirror; how 'blocked' (permission prompt) states are surfaced (toast/bell like deployment-notifications?). Spawn UX: agents page / new-session flow — how the user picks the CLI runtime (runtime selector exists via registry), what extra fields (repo to clone, subscription-token presence check with setup instructions if missing).
3. BFF spawn-path changes: spawn.ts handling for the new family (ownsSandbox=true lane; skip goal MCP auto-wire? goal tools COULD still be wired via .mcp.json — decide), session row fields (sandboxName/runtimeSandboxName for terminal targeting), repo-attach for CLI sessions (clone into pod workspace at seed — reuse mountSessionRepositories pattern or hooks-side clone), stop button -> Lifecycle Controller flow, fork semantics (probably disabled v1).
4. Per-user token UX: settings page surface for storing the Claude subscription token (paste from 'claude setup-token'), stored encrypted (which existing primitive: vaults table is workspace-scoped CMA parity; app_connections is AP-credential oriented; pick + justify), validation ping, expiry display, and spawn-time failure UX when token missing/expired.
5. Multi-CLI future: how the same surfaces generalize to codex/gemini (runtime picker entries, per-CLI token fields, herdr detects all three).
Read the actual code: src/lib/server/ws-terminal-proxy.ts + any kube-exec proxy, src/lib/components/sandbox/sandbox-terminal.svelte, openshell-terminal-tabs.svelte, sessions/[id] page, spawn.ts, runtime-registry.ts, vaults schema in src/lib/server/db/schema.ts. Ground every step in real file/function names. Return a concrete step plan.`, { label: 'plan:ux-terminal', phase: 'Design', agentType: 'Plan', schema: PLAN }),
])

return { runtime, platform, ux }