export const meta = {
  "name": "preview-gan-ui-feature",
  "description": "Generic UI-feature/refactor GAN loop running INSIDE a Tier-2 vcluster preview whose workflow-builder pod is ADOPTED by a hot-reload dev server (adopt:true: the dev pod becomes the BFF and the preview's own URL serves live edits). The Planner (gan-generator-ultracode: Opus 4.8 at ultracode effort) writes a TESTABLE JSON contract; a two-pass design_review critiques the token system BEFORE any code; then a refine loop has the GENERATOR pull the live source via GET /__export, edit ONLY src/, PUSH via POST /__sync (HMR), and smoke-check the routes — a deterministic gate then runs check + boundaries + test-unit against a full checkout of the synced src (this preview has NO /__run endpoint), while a skeptical Playwright critic LOGS IN and grades each evaluationRoute against the contract (default-reject) and writes a file-based verdict. Each iteration is captured as a promotable tar-overlay snapshot; when outputMode=pr the final source is opened as a pull request on PittampalliOrg/workflow-builder via the dev/preview-promote action. Kept GENERIC — the specific task is the run-time `intent`. No HITL/approval gate.",
  "phases": [
    {
      "title": "Dev mode"
    },
    {
      "title": "Plan"
    },
    {
      "title": "Refine"
    },
    {
      "title": "Promote"
    }
  ],
  "input": {
    "type": "object",
    "properties": {
      "intent": {
        "type": "string",
        "title": "What to build/change"
      },
      "service": {
        "type": "string",
        "default": "workflow-builder"
      },
      "generatorAgent": {
        "type": "string",
        "default": "gan-generator-ultracode"
      },
      "criticAgent": {
        "type": "string",
        "default": "gan-critic-claude"
      },
      "maxIterations": {
        "type": "number",
        "default": 5
      }
    }
  }
}

// GENERATED by scripts/fixtures/generator-critic/gen/gan-script-generator.ts —
// never hand-edit. Personas/prompts come from fragments/personas.ts, shared
// verbatim with the SW emitter so the two producers cannot drift.
const GATE_COMMAND = "IDX=__IDX__\nEXPORT_URL=__EXPORT_URL__\nSYNC_TOKEN=__SYNC_TOKEN__\nexport CI=1\nset +e\nmkdir -p /sandbox/work/gan /sandbox/scratch\nREPO=/sandbox/scratch/gate-repo\nNM_CACHE=/sandbox/work/gan/gate-node-modules.tar\nGATE_JSON=/sandbox/work/gan/gate-$IDX.json\n# pnpm bootstrap for the NON-ROOT cli-agent user: the workspace pod has\n# node/npm/corepack but no pnpm, and the user cannot write /usr/local/bin or the\n# global npm prefix — so install into user-writable /tmp dirs and put them on\n# PATH BEFORE any phase runs. Pin pnpm@10 (the repo Dockerfile's version): the\n# repo has NO packageManager pin, and corepack's pnpm 11 refuses dependency build\n# scripts (ERR_PNPM_IGNORED_BUILDS). /tmp/gan-npm/bin is FIRST on PATH so the\n# pinned pnpm@10 wins over any corepack shim.\nexport PATH=/tmp/gan-npm/bin:/tmp/gan-bin:$PATH\nif ! command -v pnpm >/dev/null 2>&1; then\n  mkdir -p /tmp/gan-bin /tmp/gan-npm\n  npm i -g --prefix /tmp/gan-npm pnpm@10 >/dev/null 2>&1 || corepack enable --install-directory /tmp/gan-bin >/dev/null 2>&1\nfi\nrm -rf \"$REPO\"\nif ! git clone --depth 1 --single-branch https://x-access-token:${GITHUB_TOKEN}@github.com/PittampalliOrg/workflow-builder.git \"$REPO\"; then\n  echo \"OBJECTIVE FAIL: clone failed\"\n  printf '{\"pass\":false,\"phases\":{\"clone\":1},\"iteration\":%s}' \"$IDX\" > \"$GATE_JSON\"\n  exit 0\nfi\nif ! curl -sS -H \"x-sync-token: $SYNC_TOKEN\" \"$EXPORT_URL\" | tar -xz -C \"$REPO\"; then\n  echo \"OBJECTIVE FAIL: export overlay failed\"\n  printf '{\"pass\":false,\"phases\":{\"overlay\":1},\"iteration\":%s}' \"$IDX\" > \"$GATE_JSON\"\n  exit 0\nfi\ncd \"$REPO\"\n[ -f \"$NM_CACHE\" ] && tar -xf \"$NM_CACHE\" -C \"$REPO\" 2>/dev/null\n# Run each phase HERMETICALLY. env -i strips leaked agent-pod secrets\n# (CLAUDE_CODE_OAUTH_TOKEN / CODEX_AUTH_JSON / AGY_AUTH_JSON / internal tokens /\n# MLflow env) that make the repo's env-isolation unit tests fail; it keeps only\n# PATH (carries /tmp/gan-npm/bin for pnpm@10), HOME, CI, a right-sized\n# NODE_OPTIONS heap (svelte-check SIGABRTs V8 under the ~6GiB cgroup otherwise),\n# and TMPDIR. The pnpm@10 bootstrap above stays OUTSIDE this sanitized env.\nrun_phase() { # name timeout logfile cmd...\n  ph_name=$1; ph_to=$2; ph_log=$3; shift 3\n  env -i PATH=\"$PATH\" HOME=\"$HOME\" CI=1 NODE_OPTIONS=--max-old-space-size=4096 TMPDIR=/tmp timeout \"$ph_to\" \"$@\" >\"$ph_log\" 2>&1\n  ph_rc=$?\n  echo \"--- $ph_name rc=$ph_rc\"; tail -25 \"$ph_log\"\n  return $ph_rc\n}\nIRC=0\nif [ ! -d node_modules ]; then\n  run_phase install 300 /tmp/gate-install.log pnpm install --no-frozen-lockfile --ignore-scripts; IRC=$?\n  [ \"$IRC\" -eq 0 ] && tar -cf \"$NM_CACHE\" node_modules 2>/dev/null\nelse\n  echo \"--- install rc=0 (cached node_modules)\"\nfi\nrun_phase check 420 /tmp/gate-check.log pnpm check; CRC=$?\nrun_phase boundaries 180 /tmp/gate-bound.log pnpm check:boundaries; BRC=$?\nrun_phase test-unit 420 /tmp/gate-test.log pnpm test:unit; TRC=$?\nexport IRC CRC BRC TRC GATE_IDX=$IDX GATE_JSON\npython3 - <<'PYZZ'\nimport json,os\nphases={\"install\":int(os.environ.get(\"IRC\",\"0\") or 0),\"check\":int(os.environ.get(\"CRC\",\"0\") or 0),\"boundaries\":int(os.environ.get(\"BRC\",\"0\") or 0),\"test-unit\":int(os.environ.get(\"TRC\",\"0\") or 0)}\nok=all(v==0 for v in phases.values())\njson.dump({\"pass\":ok,\"phases\":phases,\"iteration\":int(os.environ.get(\"GATE_IDX\",\"0\") or 0)},open(os.environ[\"GATE_JSON\"],\"w\"))\nPYZZ\nif [ \"$IRC\" -eq 0 ] && [ \"$CRC\" -eq 0 ] && [ \"$BRC\" -eq 0 ] && [ \"$TRC\" -eq 0 ]; then\n  echo \"OBJECTIVE PASS: install+check+boundaries+test-unit OK\"\nelse\n  echo \"OBJECTIVE FAIL: install rc=$IRC check rc=$CRC boundaries rc=$BRC test-unit rc=$TRC\"\nfi\n"
const t = args ?? {}
const service = t.service ?? "workflow-builder"
const generatorAgent = t.generatorAgent ?? "gan-generator-ultracode"
const criticAgent = t.criticAgent ?? "gan-critic-claude"
const maxIterations = Number(t.maxIterations ?? 5) || 5

phase('Dev mode')
// preview-native adopt → the runner child's durable ready-set poll (B1).
const preview = await action('dev/preview', {
  service,
  mode: 'preview-native',
  adopt: true,
  timeoutSeconds: 86400,
  waitReadySeconds: 240,
}, { label: 'enter_dev_mode' })

const PLAN_SANDBOX = { workspaceRef: workspace, cwd: '/sandbox/scratch' }

phase('Plan')
const plan = await agent("${ \"You are the PLANNER for a UI feature/refactor on the workflow-builder app (Anthropic GAN harness). Stay HIGH-LEVEL.\\n\\nThis is a HEADLESS automated run — there is NO human. NEVER use AskUserQuestion, NEVER wait for permission or input. When you are blocked, write a concise diagnosis as your final message and STOP the turn; the harness reads your message and the loop continues.\\n\\n=== HARD RULE: DO NOT WRITE APP CODE ===\\nThis step does NOT sync anything. Any edits to app source live only in throwaway /sandbox/scratch and are DISCARDED — a separate GENERATOR builds the real code later. Read the current UI for understanding ONLY. Your ONE persistent deliverable is /sandbox/work/contract.json.\\n\\nFeature/refactor request:\\n\" + ((.trigger.intent // \"improve the UI\")) + \"\\n\\nRoutes to evaluate: \" + ((.trigger.evaluationRoutes // [\"/dashboard\"]) | join(\", \")) + \"\\nEXPORT_URL=\" + ((.enter_dev_mode.url // \"\") + \"/__export\") + \"\\nSYNC_TOKEN=\" + (.enter_dev_mode.syncCapability // \"\") + \"\\n\\nSTEPS: (1) FIRST pick a writable scratch root: run `SCRATCH=/sandbox/scratch; [ -w /sandbox/scratch ] || SCRATCH=/tmp/scratch; mkdir -p \\\"$SCRATCH\\\"` and use \\\"$SCRATCH/repo\\\" as your staging dir for the rest of these steps (never assume /sandbox/scratch exists). Then rm -rf \\\"$SCRATCH/repo\\\" && mkdir -p \\\"$SCRATCH/repo\\\" && curl -sS -H \\\"x-sync-token: $SYNC_TOKEN\\\" \\\"$EXPORT_URL\\\" | tar -xz -C \\\"$SCRATCH/repo\\\" and READ the current source for the evaluation routes under \\\"$SCRATCH/repo/src\\\" (understand the data/links/forms/components to preserve) — do not edit. (2) mkdir -p /sandbox/work and WRITE /sandbox/work/contract.json (strict JSON), then cat it to verify. The contract has keys: objective (one sentence); subject, audience, featureJob; acceptanceCriteria (array of 5-8 {id,description,verify} TESTABLE on the live page — cover the feature's core behavior, REAL data with graceful empty states, existing functionality staying intact, accessibility (AA contrast + visible keyboard focus), responsiveness down to mobile, and visual/interaction quality); designTokens {palette:4-6 {name,hex} (deliberate, NOT a generic AI default), typography {display,body,utility}, wireframe (ASCII), signature (the one memorable element)}; rubric (string: penalize AI-default looks; require deliberate type/spacing/contrast, real data with graceful empty states, active-voice copy, and hexagonal-architecture discipline for any server/data code). (3) STOP after writing + verifying the file. Do NOT write app code.\" }", {
  label: 'plan',
  agent: generatorAgent,
  isolation: 'shared',
  sandbox: { ...PLAN_SANDBOX, maxTurns: 12, timeoutMinutes: 20 },
})

const designReview = await agent("${ \"Second-pass design review BEFORE any code. Feature/refactor request:\\n\" + ((.trigger.intent // \"\")) + \"\\n\\nRead /sandbox/work/contract.json, review its designTokens against the rubric, and write your verdict to /sandbox/work/design-review.json per your instructions (strict JSON {approved, feedback}).\" }", {
  label: 'design_review',
  agent: criticAgent,
  isolation: 'shared',
  sandbox: { ...PLAN_SANDBOX, maxTurns: 8, timeoutMinutes: 15 },
})

phase('Refine')
// The SW `for` + jq `while` gate becomes a plain loop: iterate until the
// critic accepts or the iteration cap is hit (the same bound the spec used).
const VERDICT_SCHEMA = {
  type: 'object',
  required: ['accepted'],
  properties: {
    accepted: { type: 'boolean' },
    score: { type: 'number' },
    failing: { type: 'array', items: { type: 'string' } },
    summary: { type: 'string' },
  },
}

let accepted = false
let iterations = 0
let lastVerdict = null
let lastGenerator = null

while (!accepted && iterations < maxIterations) {
  const feedback = lastVerdict
    ? `\n\nPREVIOUS CRITIC VERDICT (address every failing item):\n${JSON.stringify(lastVerdict)}`
    : ''

  lastGenerator = await agent("${ \"You are the GENERATOR/builder. You are NOT the critic; a separate critic session grades your work. Never write verdict files; never grade.\\n\\nImplement the requested UI feature/refactor on the workflow-builder app and PUSH it live.\\n\\nThis is a HEADLESS automated run — there is NO human. NEVER use AskUserQuestion, NEVER wait for permission or input. When you are blocked, write a concise diagnosis as your final message and STOP the turn; the harness reads your message and the loop continues.\\n\\nHARD REQUIREMENT — satisfy the contract's acceptanceCriteria + designTokens + rubric and address the latest critic feedback. Wire REAL data via +page.server.ts from EXISTING repo endpoints (grep src for the real ones); NEVER fabricate data; guard EACH server-side source independently (try/catch or Promise.allSettled) and degrade THAT region to a graceful empty state — one failing source must never 500 the page. Keep hexagonal-architecture discipline. Keep existing functionality working; do NOT touch the sign-in/auth pages.\\n\\nECOSYSTEM SCOPE — this preview is fully isolated, so beyond the feature, if anything in the app's OWN code blocks or degrades the evaluated routes (a data-shape bug in a server adapter, a broken shared component, an app-shell defect like the nav not collapsing on mobile), FIX IT TOO: keep the change minimal and tested, the gate must stay green, and NEVER touch the sign-in/auth pages. Do not declare in-app issues out of scope.\\n\\nKNOWN LANDMINE in this repo: list endpoints (dashboard recent-changes/agents/runs) can return entries with DUPLICATE ids (same resource, multiple versions). In Svelte 5 a keyed {#each} with a duplicate key throws each_key_duplicate during hydration and unmounts the entire subtree even though SSR looks fine. ALWAYS dedupe lists in the server load AND key {#each} blocks by a guaranteed-unique composite (id + version/index).\\n\\nOptionally read /sandbox/work/contract.json and /sandbox/work/design-review.json IF they exist (do NOT fail if missing).\\n\\nEXPORT_URL=\" + ((.enter_dev_mode.url // \"\") + \"/__export\") + \"\\nSYNC_URL=\" + (.enter_dev_mode.syncUrl // \"\") + \"\\nSYNC_TOKEN=\" + (.enter_dev_mode.syncCapability // \"\") + \"\\nPREVIEW_URL=\" + (.enter_dev_mode.url // \"\") + \"\\nTARGET_ROUTES=\" + ((.trigger.evaluationRoutes // [\"/dashboard\"]) | join(\", \")) + \"\\nLOGIN_EMAIL=\" + (.trigger.previewLogin // \"admin@example.com\") + \"\\nLOGIN_PASSWORD=\" + (.trigger.previewPassword // \"developer\") + \"\\n\\nFEATURE/INTENT:\\n\" + ((.trigger.intent // \"\")) + \"\\n\\nSTEPS each turn: (1) FIRST pick a writable scratch root: run `SCRATCH=/sandbox/scratch; [ -w /sandbox/scratch ] || SCRATCH=/tmp/scratch; mkdir -p \\\"$SCRATCH\\\"` and use \\\"$SCRATCH/repo\\\" as your staging dir for the rest of these steps (never assume /sandbox/scratch exists). Then rm -rf \\\"$SCRATCH/repo\\\" && mkdir -p \\\"$SCRATCH/repo\\\"; pull the COMPLETE receiver-owned tree and retain its exact root contract: curl -sS -H \\\"x-sync-token: $SYNC_TOKEN\\\" -D \\\"$SCRATCH/export.headers\\\" \\\"$EXPORT_URL\\\" -o \\\"$SCRATCH/source.tgz\\\" && ROOTS_JSON=\\\"$(sed -n 's/^x-sync-roots:[[:space:]]*//p' \\\"$SCRATCH/export.headers\\\" | tr -d '\r' | tail -1)\\\" && printf '%s' \\\"$ROOTS_JSON\\\" | jq -e 'type == \\\"array\\\" and length > 0' >/dev/null && tar -xzf \\\"$SCRATCH/source.tgz\\\" -C \\\"$SCRATCH/repo\\\". (2) edit ONLY files under \\\"$SCRATCH/repo/src\\\" to satisfy the contract for the target routes. (3) push live as one identified atomic generation: cd \\\"$SCRATCH/repo\\\" && GEN=\\\"$(cat /proc/sys/kernel/random/uuid)\\\" && printf '%s' \\\"$ROOTS_JSON\\\" | jq -r '.[]' > \\\"$SCRATCH/declared-roots\\\" && : > \\\"$SCRATCH/existing-roots\\\" && while IFS= read -r p; do [ ! -e \\\"$p\\\" ] || printf '%s\\n' \\\"$p\\\" >> \\\"$SCRATCH/existing-roots\\\"; done < \\\"$SCRATCH/declared-roots\\\" && tar -czf \\\"$SCRATCH/sync.tgz\\\" -T \\\"$SCRATCH/existing-roots\\\" && curl -sS -X POST --data-binary @\\\"$SCRATCH/sync.tgz\\\" -H 'content-type: application/gzip' -H \\\"x-sync-token: $SYNC_TOKEN\\\" -H \\\"x-sync-generation: $GEN\\\" -H 'x-sync-service: workflow-builder' -H \\\"x-sync-roots: $ROOTS_JSON\\\" \\\"$SYNC_URL\\\". Never reuse one generation with different bytes. (4) SMOKE before you STOP — this preview has NO /__run endpoint, so NEVER call /__run (it 404s and checks nothing). A 302 → /auth/sign-in on app routes is HEALTHY (the auth guard), never a failure — do NOT go investigate it. For an AUTHENTICATED smoke (preferred): sign in ONCE to get a cookie jar — curl -sS -c /tmp/cj -H 'content-type: application/json' \" + (\"-d '\" + ({email: (.trigger.previewLogin // \"admin@example.com\"), password: (.trigger.previewPassword // \"developer\")} | tojson) + \"' \") + \"$PREVIEW_URL/api/v1/auth/sign-in — then curl -b /tmp/cj each route in TARGET_ROUTES and confirm HTTP 200 with no top-level ReferenceError and no each_key_duplicate in the served HTML; if a route 500s or crashes on mount, fix it (usually an unguarded server load or a missing import) before you STOP. Do NOT touch the sign-in page code itself. A deterministic gate step then runs pnpm check + check:boundaries + test-unit against a full checkout of your synced src, and a separate Playwright critic grades the live routes — you do NOT self-grade.\\n\\nDeterministic gate result from your LAST synced src (fix any check / boundaries / test-unit failures in src/ this turn; empty on the first turn):\\n\" + (((.loop.last.gate.result.stdout // .loop.last.gate.stdout // .loop.last.gate.result.output // .loop.last.gate.output // \"\") | if . == \"\" then \"(gate not run yet)\" else . end)) + \"\\n\\nYour previous attempt:\\n\" + (.loop.last.generate.content // \"(first attempt)\") + \"\\n\\nCritic feedback to address now:\\n\" + (((((.loop.last.read_verdict.result.stdout // .loop.last.read_verdict.stdout // .loop.last.read_verdict.result.output // .loop.last.read_verdict.output // \"{}\") | fromjson?) // {}) | (.feedback // \"\")) as $f | (if $f == \"\" then \"(none yet — make your first improvement)\" else $f end)) + \"\\n\\nEcosystem issues flagged by the critic (in-app, outside the feature — fix these too if reasonable, keeping the gate green):\\n\" + (((((.loop.last.read_verdict.result.stdout // .loop.last.read_verdict.stdout // .loop.last.read_verdict.result.output // .loop.last.read_verdict.output // \"{}\") | fromjson?) // {}) | (.ecosystem // [])) as $e | (if ($e | length) == 0 then \"(none)\" else ($e | tojson) end)) }" + feedback, {
    label: `generate #${iterations + 1}`,
    phase: 'Refine',
    agent: generatorAgent,
    isolation: 'shared',
    sandbox: { ...PLAN_SANDBOX, maxTurns: 40, timeoutMinutes: 45 },
  })

  // Deterministic build gate — the SAME shell the SW emitter builds
  // (fragments/gate.ts); its jq placeholders become JS interpolation.
  const gateCommand = GATE_COMMAND
    .split('__IDX__').join(String(iterations))
    .split('__EXPORT_URL__').join((preview?.url ?? '') + '/__export')
    .split('__SYNC_TOKEN__').join(preview?.syncCapability ?? '')
  const gate = await action('workspace/command', {
    cliWorkspace: true,
    helperPod: true,
    helperTimeoutMinutes: 240,
    workspaceRef: workspace,
    command: gateCommand,
    cwd: "/sandbox/work",
    timeoutMs: 1500000,
  }, { label: `gate #${iterations + 1}`, phase: 'Refine', allowFailure: true })

  // allowFailure envelope: a FAILING action nests its payload under `data`
  // (a succeeding one returns it directly) — unwrap both.
  const gateBase = gate?.data ?? gate ?? {}
  const gateRes = gateBase.result ?? gateBase
  const gateOk = (gateRes.exitCode ?? gateBase.exitCode ?? 1) === 0
  if (!gateOk) {
    lastVerdict = {
      accepted: false,
      failing: ['build gate failed: ' + String(gateRes.stderr ?? gateRes.stdout ?? '').slice(0, 800)],
      summary: 'build gate failed',
    }
    iterations += 1
    continue
  }

  lastVerdict = await agent("${ \"Grade the LIVE app after the feature/refactor using your Playwright MCP tools. The app is AUTHENTICATED — log in FIRST.\\n\\nThis is a HEADLESS automated run — there is NO human. NEVER use AskUserQuestion, NEVER wait for permission or input. When you are blocked, write a concise diagnosis as your final message and STOP the turn; the harness reads your message and the loop continues.\\n\\nPREVIEW_URL=\" + (.enter_dev_mode.url // \"\") + \"\\nEVAL_ROUTES=\" + ((.trigger.evaluationRoutes // [\"/dashboard\"]) | join(\", \")) + \"\\nLOGIN_EMAIL=\" + (.trigger.previewLogin // \"admin@example.com\") + \"\\nLOGIN_PASSWORD=\" + (.trigger.previewPassword // \"developer\") + \"\\nITERATION=\" + (.idx | tostring) + \"\\n\\nREADINESS — the dev server RESTARTS on every /__sync, so the preview URL flaps between grades. BEFORE you navigate to grade EACH route, poll it until it is back: GET PREVIEW_URL/api/health (an unauthenticated 200-only probe) and retry for up to ~90s until HTTP 200, then load the route. A transient 502/503/connection-reset/blank page during that window is the restart, NOT a defect — wait it out and retry.\\n\\nOpen PREVIEW_URL/auth/sign-in (slow first load — use a 180000 ms navigation timeout + retry once or twice), LOG IN with the credentials above, then visit EACH route in EVAL_ROUTES. Screenshot desktop AND mobile, and interact (expand/hover/drill-down/keyboard focus) to check the feature.\\n\\nCLASSIFY every problem into exactly one bucket. (1) FEATURE defects (the change the generator made) → perRoute + feedback; these gate acceptance. (2) IN-APP issues that live in THIS repo's own src but are OUTSIDE the feature — a broken shared component, a data-shape bug in a server adapter, an app-shell defect like the mobile nav never collapsing, or a favicon 404 the app itself serves — → the ecosystemIssues array as {area,detail,suggestedFix}. These are REQUIRED reporting but do NOT lower score or block meets_criteria. (3) INFRASTRUCTURE failures OUTSIDE the app's code — other services returning 5xx (e.g. /metrics), DB/cluster/timeout, preview machinery — → the envIssues array; do NOT lower score for these. HARD BOUNDARY: if it lives in this repo's src it is NEVER an env issue; a failure that also reproduces on an unchanged route is an in-app (ecosystem) issue, not a feature defect. Grade the CHANGE the generator made.\\n\\nIf /sandbox/work/contract.json exists, grade against its acceptanceCriteria + rubric; if MISSING, grade the feature request above anyway — NEVER refuse to grade for a missing contract file. Check: the feature behaves correctly, shows REAL data or a graceful empty state, existing functionality is intact, accessibility (AA contrast + visible keyboard focus), responsiveness, and visual/interaction quality (deliberate palette/typography/spacing, NO generic AI-default look).\\n\\nVERDICT FILE (do this FIRST, before your final message): mkdir -p /sandbox/work/gan and write the EXACT strict JSON verdict object below — PLUS two extra keys \\\"iteration\\\": \" + (.idx | tostring) + \" and \\\"schema\\\": \\\"gan.verdict/v1\\\" — to /sandbox/work/gan/verdict-\" + (.idx | tostring) + \".json. THEN emit the identical verdict JSON as your final message. The file is the primary loop-exit signal; the message is the fallback.\\n\\nYour ENTIRE final message must be ONLY this strict JSON object (start with { and end with }, NO prose/preamble and NO markdown fences; put ALL reasoning inside feedback) — the harness machine-parses your FINAL message to decide whether the loop stops: {\\\"meets_criteria\\\": <true|false>, \\\"score\\\": <0-10>, \\\"perRoute\\\": [{\\\"route\\\": <string>, \\\"passes\\\": <bool>, \\\"note\\\": <string>}], \\\"ecosystemIssues\\\": [{\\\"area\\\": <string>, \\\"detail\\\": <string>, \\\"suggestedFix\\\": <string>}], \\\"envIssues\\\": [{\\\"route\\\": <string>, \\\"detail\\\": <string>}], \\\"feedback\\\": \\\"<specific, actionable next-step guidance>\\\"}. Set meets_criteria true ONLY if EVERY evaluation route meets the criteria, the feature works with real/empty-state data, existing functionality is intact, and score is at least 8. In-app issues you recorded in ecosystemIssues and infrastructure failures you recorded in envIssues MUST NOT lower score or block meets_criteria.\" }", {
    label: `critique #${iterations + 1}`,
    phase: 'Refine',
    agent: criticAgent,
    isolation: 'shared',
    schema: VERDICT_SCHEMA,
    sandbox: { ...PLAN_SANDBOX, maxTurns: 25, timeoutMinutes: 30 },
  })

  accepted = lastVerdict?.accepted === true
  iterations += 1
}

phase('Promote')
// Promote → PR only on acceptance (the SW spec gated this with jq).
let promote = null
if (accepted) {
  promote = await action('dev/preview-promote', {
    service,
    title: t.intent ?? 'GAN: accepted change',
  }, { label: 'promote', allowFailure: true })
}

return {
  accepted,
  iterations,
  service,
  preview,
  verdict: lastVerdict,
  generatorSummary: typeof lastGenerator === 'string' ? lastGenerator.slice(0, 4000) : lastGenerator,
  designReview: typeof designReview === 'string' ? designReview.slice(0, 4000) : designReview,
  plan: typeof plan === 'string' ? plan.slice(0, 4000) : plan,
  promote,
}
