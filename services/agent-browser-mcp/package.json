{
  "name": "agent-browser-mcp",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Vercel agent-browser as a streamable-HTTP MCP server (Chrome-isolated) for non-CLI (dapr-agent-py) agents, with deterministic artifact persistence to the owning run.",
  "scripts": {
    "start": "node bridge.mjs",
    "test": "node --test *.node-test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.26.0",
    "agent-browser": "0.31.2",
    "express": "5.1.0"
  }
}
