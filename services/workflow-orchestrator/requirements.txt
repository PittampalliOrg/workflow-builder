fastapi>=0.115.0
uvicorn>=0.32.0
dapr==1.18.0
dapr-ext-workflow==1.18.0
pydantic>=2.0.0
requests>=2.31.0
httpx>=0.27.0
psycopg2-binary>=2.9.0
jq>=1.8.0
cel-python>=0.3.0

# OpenTelemetry (traces + metrics + log correlation)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0
opentelemetry-instrumentation-requests>=0.41b0
opentelemetry-instrumentation-httpx>=0.41b0
opentelemetry-instrumentation-logging>=0.41b0

# MLflow tracing destination (P7 of the cutover, 2026-05-11). Powers
# mlflow.tracing.set_destination() in tracing.py so workflow spans land
# in MLflow with the trace_request_metadata rows that mlflow.search_traces()
# requires. mlflow-skinny excludes scikit-learn / pandas / etc.; we only
# need the tracing module + OTEL bridge.
mlflow-skinny>=3.6
