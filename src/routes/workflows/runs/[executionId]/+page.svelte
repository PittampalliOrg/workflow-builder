<script lang="ts">
	// This page should never render — the server-side load() always redirects.
	// Rendered briefly only if a client navigates after a server cache miss.
</script>

<div class="p-6 text-sm text-muted-foreground">Redirecting to run detail…</div>
